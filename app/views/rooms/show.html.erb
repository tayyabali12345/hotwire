<p style="color: green"><%= notice %></p>

<%# here to appear message on multple screen simultaneously %>
<%= turbo_stream_from @room %>

<%= turbo_frame_tag "room" do%>
  <%= render @room %>

  <p>
    <%= link_to "Edit this room", edit_room_path(@room) %> |
    <%= link_to "Back to rooms", rooms_path, "data-turbo-frame": "_top" %>

    <%= button_to "Destroy this room", @room, method: :delete %>
  </p>
<%end%>


<div id="messages">
<ul data-controller= "position">
  <%= render @room.messages%>
</ul>
</div>

<%# <%= link_to "New message", new_room_message_path(@room)%>
<%= turbo_frame_tag "new_message", src: new_room_message_path(@room), target: "_top"%>
